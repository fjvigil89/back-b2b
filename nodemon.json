{
  "watch": ["**/*.ts"],
  "ext": "ts",
  "ignore": ["./test/*.ts"],
  "exec": "node -r ts-node/register -r dotenv/config src/index.ts dotenv_config_path=$(pwd)/.env.dev",
  "env": {
    "NODE_ENV": "development"
  }
}
